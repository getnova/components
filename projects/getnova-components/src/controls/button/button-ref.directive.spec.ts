import { ButtonRefDirective } from './button-ref.directive';

describe('ButtonRefDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonRefDirective();
    expect(directive).toBeTruthy();
  });
});
