import {CheckboxRefDirective} from './checkbox-ref.directive';

describe('CheckboxRefDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckboxRefDirective();
    expect(directive).toBeTruthy();
  });
});
