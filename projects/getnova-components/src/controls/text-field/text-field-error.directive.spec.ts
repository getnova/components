import {TextFieldErrorDirective} from './text-field-error.directive';

describe('TextFieldErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new TextFieldErrorDirective();
    expect(directive).toBeTruthy();
  });
});
